# Provider 单价（美元 / 1K tokens），用于成本追踪
# 价格为近似参考值，请按各厂商最新定价调整
minimax:
  input_per_1k: 0.01
  output_per_1k: 0.03
deepseek:
  input_per_1k: 0.001
  output_per_1k: 0.002
doubao:
  input_per_1k: 0.0008
  output_per_1k: 0.002
qwen:
  input_per_1k: 0.002
  output_per_1k: 0.006

# TTS 单价（美元 / 1K 字符），用于成本追踪（v0.6.2）
# minimax t2a_v2 按字符计费，rate 未核实 -> 0.0 占位（结构对，数值待填实测）
minimax_tts:
  per_1k_chars: 0.0
